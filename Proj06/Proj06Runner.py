def run(str, sep):
    """Returns a substring of any given string based upon a given range of characters before and after the substring"""

    student_name = 'Mark Newman'
    separation_point = str.index(sep)

    aList = [student_name, str]
        
    return aList